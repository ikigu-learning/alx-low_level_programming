#include <stdio.h>

/**
  * _puthchar - prints a character to standard output
  * @c: the character to be printed
  *
  * Return: Always 0 (Success)
  */

int _putchar(int c)
{
	putchar(c);
	return 0;
}
